let subsType = "Free";

if (subscriptionType === "Premium") {
  console.log("You have access to all the features");
} else if (subscriptionType === "Free") {
  console.log("You have access to limited features");
} else {
  console.log("not a valid subscription");
}
